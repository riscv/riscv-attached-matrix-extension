[[programming_model]]
== Programming Model
:imagesdir: ../images

=== Instruction stream

AME instructions are in-line with other instructions on the hart.
They are executed in program order, though follow a weaker memory model than RVWMO such that
AME memory operations not guaranteed to appear in program order relative to non-AME memory operations
without an ordering fence (see TODO).

TODO: AME state enablement.

=== State

Each hart implementing AME adds its own, private, AME registers and CSRs.
AME instructions execute on the hart to access that state and control data transfer between AME
registers and memory.

.Register state and data movement paths for AME. The F/D and V extensions are _optional_ with AME. The only architectural datapath between vector (if present) and AME is through memory. The scalar datapath(s) is/are one-way. AME accumulator state can only be moved to/from the AME matrix registers.
image::ame_state.svg[AME state and architectural datapaths]

The `Acc` registers are used to accumulate the output of partial matrix multiplies.
The `M` registers are general-purpose matrix registers.
They are used for anything that is not an accumulation, including the data operand of load or store instructions,
the input operands of 1D (element-wise/vector) operations, and the output operands of 1D operations.

A single `M` register may hold multiple, one, or part of a square,
depending on implementation-defined parameters and programmed datatype.
Generally, the number of squares that will fit in an `M` register is inversely proportional
to the datatype size (because larger datatypes require more storage).

In contrast, an `Acc` register always holds exactly one square.

=== Squares

The *pack_factor* of a datatype is the number of squares contained in a single M register:

* **Packed** (`sizeof(dtype) < AME_UNIT_DATATYPE_SIZE`): `pack_factor = AME_UNIT_DATATYPE_SIZE / sizeof(dtype)`
* **Unit or Wide** (`sizeof(dtype) >= AME_UNIT_DATATYPE_SIZE`): `pack_factor = 1`

When the programmed datatype is a *packed datatype*
(size < `<<udb:doc:param:AME_UNIT_DATATYPE_SIZE,AME_UNIT_DATATYPE_SIZE>>`),
a single M register holds `pack_factor` logical squares.

The number of elements processed by an AME matrix multiply instruction depends on the datatype.
For non-packed datatypes (`pack_factor = 1`), the operands are `N x N` squares.
For packed datatypes (`pack_factor > 1`), the input operands span `N x (pack_factor * N)` and `(pack_factor * N) x N`
elements respectively, while the accumulator output remains `N x N`.

[NOTE]
--
This constraint keeps the ISA simple: instruction encoding does not need per-operand shape fields
and register mapping is uniform. The accumulator operand always holds exactly one `N x N` square.

For contrast, general matrix multiply on non-square inputs (`M x K` by `K x N`) produces `M x N`.
AME handles this by tiling into squares and zero-padding boundary tiles in software.

.Example (`N = 8`)
* Multiply `A(3x5)` by `B(5x2)`.
* Software packs `A` and `B` into zero-padded `8x8` squares.
* AME computes a full `8x8` result square.
* Software keeps only the valid `3x2` upper-left region and discards padded rows/columns.
+
image::ame-square-padding-example.svg[Matrix padding example for N=8]

Larger non-square problems are handled the same way: process full interior `N x N` tiles normally,
and pad only edge tiles.
--

The row/column dimension, N, is implementation-defined and is equal to the
square root of <<udb:doc:param:AME_NELEM,AME_NELEM>>.
<<udb:doc:param:AME_NELEM,AME_NELEM>> can be any power of 4.

[NOTE]
--
It is possible to create portable AME binaries regardless of the chosen value of
<<udb:doc:param:AME_NELEM,AME_NELEM>>.

Highly-tuned binaries may produce a better result by targeting a specific value of
<<udb:doc:param:AME_NELEM,AME_NELEM>>, similar to how auto-vectorizing compilers can generate
more efficient code for RISC-V Vector when VLEN is known.
--

All elements within a single square have the same datatype, for example unsigned 8-bit integer or
single-precision IEEE floating point.

A single operation may reference squares with different datatypes (for example, 8-bit integer inputs widening to a 16-bit integer output).
Mixed-datatype operations are optional; see <<datatypes>> for the implementation discovery mechanism.

[[mapping-squares-to-m-registers]]
==== Mapping squares to M registers

A single square (vector or matrix) may fit in one `M` register or may span multiple consecutive
`M` registers, depending on the datatype size.
If the datatype is no wider than <<udb:doc:param:AME_UNIT_DATATYPE_SIZE,AME_UNIT_DATATYPE_SIZE>>,
one register is sufficient;
wider datatypes require additional registers.
The required register count is the datatype size rounded up in units of
<<udb:doc:param:AME_UNIT_DATATYPE_SIZE,AME_UNIT_DATATYPE_SIZE>>
(that is, `ceil(sizeof(datatype)/AME_UNIT_DATATYPE_SIZE)`).

When a square requires multiple consecutive `M` registers, consecutive registers are combined into a group.
When registers are grouped, the register index must be a multiple of the group size (_e.g._, when
group size is two, register indices must be even).
Specifying a register index that is not a multiple of the group size results in an
Illegal Instruction exception.
This mechanism is analogous to the `LMUL` facility used in the RISC-V vector (`V`) extension.

Packed datatypes can be used directly as operands in compute instructions;
see <<operand-formation>> for how multi-operand instructions determine register spans.

.Square-to-M-register mapping examples
image::ame-square-reg-mapping.svg[Square to M register mapping examples]

When an `M` register is part of a group, only the lowest-indexed register of that group may be used directly in operations.
Using any other register in the group results in undefined behavior.

// not really bash, but it gets us colored # comments
[source,bash]
----
# precondition:
# a square is in m[2-3]

madd.1d m2, m2, m2  # well-defined, m2 = m2 + m2

madd.1d m2, m3, m3  # undefined
madd.1d m3, m2, m2  # undefined
----

[[operand-formation]]
==== Operand formation

When a compute instruction references multiple tensor operands, all operands must contain
the same total number of squares.
The instruction's square count is `N_sq = max(pack_factor)` across all tensor operands.

Each operand's register span is then sized to hold exactly `N_sq` squares:

* **Packed operand** (`pack_factor > 1`): `N_sq / pack_factor` registers
* **Unit or Wide operand** (`pack_factor = 1`): `N_sq x ceil(sizeof(dtype) / AME_UNIT_DATATYPE_SIZE)` registers


Consider an ADD instruction with three tensor operands of different datatypes:

.Operand formation example (`AME_UNIT_DATATYPE_SIZE` = 8 bits)
[cols="1,1,1,1,1",options="header"]
|===
| Operand | Dtype | pack_factor | Register Span | Total Squares

| Tsrc0 | Int4  | 2 | N_sq / 2 = 1 register  | 2
| Tsrc1 | Int8  | 1 | N_sq x 1 = 2 registers | 2
| Tdst  | Int16 | 1 | N_sq x 2 = 4 registers | 2
|===

`N_sq = max(2, 1, 1) = 2` (set by the Int4 operand with `pack_factor = 2`).
All three operands contain `N_sq x AME_NELEM = 2 x AME_NELEM` elements total.

Accumulator (`Acc`) operands are not subject to this rule:
they always hold exactly one square regardless of datatype.

This rule applies uniformly to both 1D (element-wise) and 2D (matrix multiply) instructions.
For 1D instructions, each operand processes `N_sq` squares element-wise — a packed
operand's `pack_factor` squares are stored contiguously in a single register, while a
non-packed operand's `N_sq` squares span `N_sq` registers.
For 2D matrix multiply instructions, the inner product dimension extends to
`pack_factor * N` elements; see <<_2_dimensional_matrix_multiply_operations>>.

=== Polymorphic instructions

AME instructions do not encode operand datatypes; instead, datatypes are determined by dedicated datatype registers.

[NOTE]
This feature makes AME analogous to polymorphic programming languages
– a single function (instruction) is defined for all datatype combinations (polymorphic argument types).

=== Datatypes

AME supports up to `2^32 - 1` unique datatypes, half of which are reserved for custom use.
The value 0 is reserved to mean uninitialized.

[NOTE]
--
The datatype registers are similar to WARL CSR fields, so implementations only need to support
enough state to represent the datatypes they support.

For example, an implementation that only supports two datatypes would only need two bits to
represent those datatypes plus uninitialized.
--

Standard datatypes and encoding rules are listed in <<datatypes>>.

Implementations may choose which standard datatypes are supported and which custom datatypes
are supported.

A legal implementation may support either zero standard datatypes or zero custom datatypes (i.e.,
all custom or all standard).

[NOTE]
--
Profiles will likely mandate a minimal set of supported datatypes.
--

Datatypes are set using the <<udb:doc:inst:msettyp,msettyp>> instruction and
read using the <<udb:doc:inst:mgettyp,mgettyp>> instruction.

=== Data movement

Data movement between AME register files, the integer (X) register file, the floating point (F)
register file, and memory are restricted in order to facilitate a detached AME execution unit.

There are no AME data movement instructions between memory and the `Acc` registers;
rather, `Acc` registers can only be moved in to or out of the `M` registers.

`M` register data can be loaded and stored from/to memory.

A scalar from the X or F register file can be sent to an `M` register.

AME does not define data movement operations between AME register files and the vector (V)
register file.
Performance-sensitive AME code should use AME 1D operations on matrix data rather than RISC-V Vector.

=== Memory

AME uses the same address space as the host hart.
In systems with virtual memory, AME loads and stores go through (H)S-mode translation and,
if applicable, G-stage translation when run within a hypervisor.

AME provides memory storage variants that operate on one of three memory layouts:

*  Row-major (`mls.rm` / `mss.rm`)
*  Column-major (`mls.cm` / `mss.cm`)
*  Opaque, implementation-defined (`mls` / `mss`)

The row-major and column-major variants are called the _interoperability_ storage instructions.
They transfer data as a contiguous block — no striding or partial-tile predication is supported.

Behavior is only well-defined when loading an opaque tensor layout that was previously stored
opaquely on the same implementation.
Generally, software should assume that loads / stores using the opaque format will be more
performant than loads / stores using one of the formats with a standard layout.

==== Consistency model

Memory transfers from/to `M` registers follow a weaker memory model than other memory transfers on
the hart (from/to the X, F, or V register files).
Informally, memory transfers involving AME state may appear out of program-order with respect to
non-AME memory transfers in the hart.
Ordering is restored with QQQ (fence.ame instruction, ldaq, strl).

=== Operators

AME supports six broad classes of arithmetic and/or logical operators:

*  2D Matrix multiply
*  1D Element-wise/vector
*  Intra-tensor (reduction/expansion/permutation)
*  Register move / data conversion
*  Scalar broadcast
*  Load/store

AME does not support general masking/predication on computational instructions, though a future extension may add support.



==== 2-dimensional matrix multiply operations

AME matrix multiplication instructions calculate the matrix product `C = A x B`.

For *non-packed datatypes* (`pack_factor = 1`), A and B are each a single `N x N` square.
For *packed datatypes* (`pack_factor > 1`), A is an `N x (pack_factor * N)` matrix (pack_factor squares laid side-by-side
in the column dimension) and B is an `(pack_factor * N) x N` matrix (pack_factor squares stacked in the row
dimension). The accumulator output C is always `N x N` regardless of `pack_factor`.

Each output element `C[i,j]` is computed as the dot product of:

* row `i` of `A` (length `pack_factor * N`), and
* column `j` of `B` (length `pack_factor * N`).

The accumulation order for additions inside each dot product is implementation-defined.
That order is fixed within a given implementation, so repeated runs on the same implementation
produce the same result.

[NOTE]
Different implementations may use different accumulation orders, and therefore may produce
different results (especially for floating-point data).

Formally:

.AME matrix multiplication
[source]
--
def perm(range): implementation-defined permutation over range

# K = N     for non-packed datatypes (pack_factor = 1)
# K = pack_factor*N  for packed datatypes     (pack_factor > 1)
K = pack_factor * N

for i from 0 to N-1:
  for j from 0 to N-1:
    C[i,j] = sum(A[i,k] * B[k,j] for k in perm(0..K-1))
--

Matrix multiplication is not commutative (`A*B != B*A` in general),
so operand order must be preserved by software using AME primitives.

AME defines [Udb::Instruction#mmul.2d, Udb::Instruction#mmulacc.2d, Udb::Instruction#mmulaccneg.2d, Udb::Instruction#mmulat.2d, Udb::Instruction#mmulatacc.2d, Udb::Instruction#mmulbt.2d, Udb::Instruction#mmulbtacc.2d, Udb::Instruction#mmulneg.2d] instructions that perform a matrix multiply.
The destination of matrix multiply operations is always an accumulator register.
The operations differ in
whether or not they accumulate into the destination register (versus overwrite it),
whether or not they transpose an input before multiplying,
and whether or not they negate a result.

[cols="1,2,3",options="header"]
|===
| Instruction   | Operation  | Description
| <<udb:doc:inst:mmul_2d,mmul.2d>>
| `C = A × B`
| Matrix multiply
| <<udb:doc:inst:mmulacc_2d,mmulacc.2d>>
| `C = C + A × B`
| Matrix multiply accumulate
| <<udb:doc:inst:mmulaccneg_2d,mmulaccneg.2d>>
| `C = C - A × B`
| Negated matrix multiply accumulate
| <<udb:doc:inst:mmulat_2d,mmulat.2d>>
| `C = A^T^ × B`
| Matrix multiply with transposed A
| <<udb:doc:inst:mmulatacc_2d,mmulatacc.2d>>
| `C = C + A^T^ × B`
| Matrix multiply with transposed A, accumulate
| <<udb:doc:inst:mmulbt_2d,mmulbt.2d>>
| `C = A × B^T^`
| Matrix multiply with transposed B
| <<udb:doc:inst:mmulbtacc_2d,mmulbtacc.2d>>
| `C = C + A × B^T^`
| Matrix multiply with transposed B, accumulate
| <<udb:doc:inst:mmulneg_2d,mmulneg.2d>>
| `C = -(A × B)`
| Negated matrix multiply
|===

==== 1-dimensional elementwise operations

AME defines 1-dimensional (elementwise/vector) operations that use the `M` registers.

Element-wise operations apply the same operator independently at each coordinate of the input squares.
For every position `(i,j)`, the instruction reads `A[i,j]` and `B[i,j]`, computes the result, and
writes it to `C[i,j]`.

There are four classes of elementwise operations: Arithmetic, Bitwise, Comparison, and Nonlinear.

Arithmetic::
[cols="1,2,3",options="header"]
|===
| Instruction | Operation |  Description
| <<udb:doc:inst:mabs_ew,mabs.ew>>
| `D[i] = \|A[i]\|`
| Elementwise absolute value
| <<udb:doc:inst:mabsdiff_ew,mabsdiff.ew>>
| `D[i] = \|A[i] - B[i]\|`
| Elementwise absolute difference
| <<udb:doc:inst:mabsdiff_ew_x,mabsdiff.ew.x>>
| `D[i] = \|c - B[i]\|`
| Elementwise scalar absolute difference
| <<udb:doc:inst:madd_ew,madd.ew>>
| `D[i] = A[i] + B[i]`
| Elementwise addition
| <<udb:doc:inst:madd_ew_x,madd.ew.x>>
| `D[i] = c + B[i]`
| Elementwise scalar addition
| <<udb:doc:inst:mhdiff_ew,mhdiff.ew>>
| `D[i] = (B[i] - A[i]) / 2`
| Elementwise half-difference
| <<udb:doc:inst:mhdiff_ew_x,mhdiff.ew.x>>
| `D[i] = (c - B[i]) / 2`
| Elementwise scalar half-difference
| <<udb:doc:inst:mmax_ew,mmax.ew>>
| `D[i] = max(A[i], B[i])`
| Elementwise maximum
| <<udb:doc:inst:mmax_ew_x,mmax.ew.x>>
| `D[i] = max(c, B[i])`
| Elementwise scalar maximum
| <<udb:doc:inst:mmean_ew,mmean.ew>>
| `D[i] = (A[i] + B[i]) / 2`
| Elementwise arithmetic mean (half-add)
| <<udb:doc:inst:mmean_ew_x,mmean.ew.x>>
| `D[i] = (c + B[i]) / 2`
| Elementwise scalar mean
| <<udb:doc:inst:mmin_ew,mmin.ew>>
| `D[i] = min(A[i], B[i])`
| Elementwise minimum
| <<udb:doc:inst:mmin_ew_x,mmin.ew.x>>
| `D[i] = min(c, B[i])`
| Elementwise scalar minimum
| <<udb:doc:inst:mmul_ew,mmul.ew>>
| `D[i] = A[i] × B[i]`
| Elementwise multiplication
| <<udb:doc:inst:mmul_ew_x,mmul.ew.x>>
| `D[i] = c × B[i]`
| Elementwise scalar multiplication
| <<udb:doc:inst:mmulacc_ew,mmulacc.ew>>
| `D[i] = D[i] + A[i] × B[i]`
| Elementwise multiply-accumulate
| <<udb:doc:inst:mmulacc_ew_x,mmulacc.ew.x>>
| `D[i] = D[i] + c × B[i]`
| Elementwise scalar multiply-accumulate
| <<udb:doc:inst:mmulaccneg_ew,mmulaccneg.ew>>
| `D[i] = D[i] - A[i] × B[i]`
| Elementwise negative multiply-accumulate
| <<udb:doc:inst:mmulaccneg_ew_x,mmulaccneg.ew.x>>
| `D[i] = D[i] - c × B[i]`
| Elementwise scalar negative multiply-accumulate
| <<udb:doc:inst:mmuladd_ew,mmuladd.ew>>
| `D[i] = A[i] + B[i] × D[i]`
| Elementwise fused multiply-add
| <<udb:doc:inst:mmuladd_ew_x,mmuladd.ew.x>>
| `D[i] = c + B[i] × D[i]`
| Elementwise scalar fused multiply-add
| <<udb:doc:inst:mmulneg_ew,mmulneg.ew>>
| `D[i] = -(A[i] × B[i])`
| Elementwise negate-multiply
| <<udb:doc:inst:mmulneg_ew_x,mmulneg.ew.x>>
| `D[i] = -(c × B[i])`
| Elementwise scalar negate-multiply
| <<udb:doc:inst:mmulsub_ew,mmulsub.ew>>
| `D[i] = A[i] - B[i] × D[i]`
| Elementwise fused multiply-subtract
| <<udb:doc:inst:mmulsub_ew_x,mmulsub.ew.x>>
| `D[i] = c - B[i] × D[i]`
| Elementwise scalar fused multiply-subtract
| <<udb:doc:inst:msub_ew,msub.ew>>
| `D[i] = B[i] - A[i]`
| Elementwise subtraction
| <<udb:doc:inst:msub_ew_x,msub.ew.x>>
| `D[i] = c - B[i]`
| Elementwise scalar subtraction
|===

Bitwise::
[cols="1,2,3",options="header"]
|===
| Instruction | Operation | Description
| <<udb:doc:inst:mand_ew,mand.ew>>
| `D[i] = A[i] & B[i]`
| Elementwise bitwise AND
| <<udb:doc:inst:mand_ew_x,mand.ew.x>>
| `D[i] = c & B[i]`
| Elementwise scalar bitwise AND
| <<udb:doc:inst:mandnot_ew,mandnot.ew>>
| `D[i] = A[i] & ~B[i]`
| Elementwise bitwise AND-NOT
| <<udb:doc:inst:mandnot_ew_x,mandnot.ew.x>>
| `D[i] = c & ~B[i]`
| Elementwise scalar bitwise AND-NOT
| <<udb:doc:inst:mor_ew,mor.ew>>
| `D[i] = A[i] \| B[i]`
| Elementwise bitwise OR
| <<udb:doc:inst:mor_ew_x,mor.ew.x>>
| `D[i] = c \| B[i]`
| Elementwise scalar bitwise OR
| <<udb:doc:inst:mornot_ew,mornot.ew>>
| `D[i] = A[i] \| ~B[i]`
| Elementwise bitwise OR-NOT
| <<udb:doc:inst:mornot_ew_x,mornot.ew.x>>
| `D[i] = c \| ~B[i]`
| Elementwise scalar bitwise OR-NOT
| <<udb:doc:inst:mxor_ew,mxor.ew>>
| `D[i] = A[i] ^ B[i]`
| Elementwise bitwise XOR
| <<udb:doc:inst:mxor_ew_x,mxor.ew.x>>
| `D[i] = c ^ B[i]`
| Elementwise scalar bitwise XOR
|===

Comparison::
[cols="1,2,3",options="header"]
|===
| Instruction | Operation | Description
| <<udb:doc:inst:mcmovge_ew,mcmovge.ew>>
| `D[i] = (A[i] >= 0) ? B[i] : D[i]`
| Elementwise two-way conditional move (non-negative)
| <<udb:doc:inst:mcmovlt_ew,mcmovlt.ew>>
| `D[i] = (A[i] < 0) ? B[i] : D[i]`
| Elementwise two-way conditional move (negative)
| <<udb:doc:inst:mcmpge_ew,mcmpge.ew>>
| `D[i] = (A[i] >= B[i]) ? -1 : 0`
| Elementwise greater-or-equal compare
| <<udb:doc:inst:mcmpge_ew_x,mcmpge.ew.x>>
| `D[i] = (c >= B[i]) ? -1 : 0`
| Elementwise scalar compare greater-or-equal
| <<udb:doc:inst:mcmplt_ew,mcmplt.ew>>
| `D[i] = (A[i] < B[i]) ? -1 : 0`
| Elementwise less-than compare
| <<udb:doc:inst:mcmplt_ew_x,mcmplt.ew.x>>
| `D[i] = (c < B[i]) ? -1 : 0`
| Elementwise scalar compare less-than
| <<udb:doc:inst:mselge_ew,mselge.ew>>
| `D[i] = (A[i] >= 0) ? B[i] : 0`
| Elementwise predicated select (non-negative)
| <<udb:doc:inst:msellt_ew,msellt.ew>>
| `D[i] = (A[i] < 0) ? B[i] : 0`
| Elementwise predicated select (negative)
|===

Nonlinear::
[cols="1,2,3",options="header"]
|===
| Instruction | Operation | Description
| <<udb:doc:inst:mexp2_ew,mexp2.ew>>
| `D[i] = 2^A[i]^`
| Elementwise base-2 exponentiation
| <<udb:doc:inst:mldexp_ew,mldexp.ew>>
| `D[i] = A[i] × 2^B[i]^`
| Elementwise left-shift scale (ldexp)
| <<udb:doc:inst:mldexp_ew_x,mldexp.ew.x>>
| `D[i] = B[i] × 2^c^`
| Elementwise scalar left-shift scale (ldexp)
| <<udb:doc:inst:mldexpacc_ew,mldexpacc.ew>>
| `D[i] = D[i] + A[i] × 2^B[i]^`
| Elementwise left-shift scale accumulate
| <<udb:doc:inst:mldexpacc_ew_x,mldexpacc.ew.x>>
| `D[i] = D[i] + B[i] × 2^c^`
| Elementwise scalar left-shift scale accumulate
| <<udb:doc:inst:mlog2_ew,mlog2.ew>>
| `D[i] = log~2~(A[i])`
| Elementwise base-2 logarithm
| <<udb:doc:inst:mlog2sub_ew,mlog2sub.ew>>
| `D[i] = log~2~(\|A[i]\|) - B[i]`
| Elementwise log2 minus bias
| <<udb:doc:inst:mlog2sub_ew_x,mlog2sub.ew.x>>
| `D[i] = log~2~(\|B[i]\|) - c`
| Elementwise log2 minus scalar bias
| <<udb:doc:inst:mrdexp_ew,mrdexp.ew>>
| `D[i] = A[i] × 2^-B[i]^`
| Elementwise right-shift scale
| <<udb:doc:inst:mrdexpacc_ew,mrdexpacc.ew>>
| `D[i] = D[i] + A[i] × 2^-B[i]^`
| Elementwise right-shift scale accumulate
| <<udb:doc:inst:msublog2_ew,msublog2.ew>>
| `D[i] = B[i] - log~2~(\|A[i]\|)`
| Elementwise bias minus log2
| <<udb:doc:inst:msublog2_ew_x,msublog2.ew.x>>
| `D[i] = c - log~2~(\|B[i]\|)`
| Elementwise scalar bias minus log2
|===

==== Reduction operations

AME reduction operations reduce values across a row of a square.

[cols="1,2,3",options="header"]
|===
| Instruction | Operation | Description
| <<udb:doc:inst:mprefixadd_col,mprefixadd.col>>
| `D[i,j] = Σ~k≤i~ A[k,j]`
| Elementwise column prefix-sum (inclusive scan)
| <<udb:doc:inst:mprefixadd_row,mprefixadd.row>>
| `D[i,j] = Σ~k≤j~ A[i,k]`
| Elementwise row prefix-sum (inclusive scan)
| <<udb:doc:inst:mprefixmax_col,mprefixmax.col>>
| `D[i,j] = max~k≤i~(A[k,j])`
| Elementwise column prefix-max (running maximum scan)
| <<udb:doc:inst:mprefixmax_row,mprefixmax.row>>
| `D[i,j] = max~k≤j~(A[i,k])`
| Elementwise row prefix-max (running maximum scan)
| <<udb:doc:inst:mreduceadd_col,mreduceadd.col>>
| `D[:,j] = Σ~k~ A[k,j]`
| Elementwise column reduce (add)
| <<udb:doc:inst:mreduceadd_row,mreduceadd.row>>
| `D[i,:] = Σ~k~ A[i,k]`
| Elementwise row reduce (add)
| <<udb:doc:inst:mreducemax_col,mreducemax.col>>
| `D[:,j] = max~k~(A[k,j])`
| Elementwise column reduce (max)
| <<udb:doc:inst:mreducemax_row,mreducemax.row>>
| `D[i,:] = max~k~(A[i,k])`
| Elementwise row reduce (max)
|===

==== Permutation operations

AME permutation operations swizzle data within a single square.

[cols="1,2,3",options="header"]
|===
| Instruction | Operation | Description
| <<udb:doc:inst:mcolunzip_ew,mcolunzip.ew>>
| `D[i,j] = A[i,2j] +
 D[i, N/2+j] = A[i,2j+1]`
| Elementwise column unzip (de-interleave)
| <<udb:doc:inst:mcolzip_ew,mcolzip.ew>>
| `D[i,2j] = A[i,j] +
 D[i,2j+1] = A[i, N/2+j]`
| Elementwise column zip (interleave)
| <<udb:doc:inst:mgather_ew,mgather.ew>>
| `D[i,j] = A[i, B[i,j]]`
| Elementwise gather (take)
| <<udb:doc:inst:mrowunzip_ew,mrowunzip.ew>>
| `D^0^[i,:] = A[2i,:] +
 D^1^[i,:] = A[2i+1,:]`
| Row unzip (de-interleave rows)
| <<udb:doc:inst:mrowzip_ew,mrowzip.ew>>
| `D[2i,:] = A[i,:] +
 D[2i+1,:] = B[i,:]`
| Row zip (interleave rows from two inputs)
| <<udb:doc:inst:mscatadd_col,mscatadd.col>>
| `D[B[i,j], j] += A[i,j]`
| Elementwise column scatter-add
| <<udb:doc:inst:mscatadd_row,mscatadd.row>>
| `D[i, B[i,j]] += A[i,j]`
| Elementwise scatter-add
| <<udb:doc:inst:mscatmax_col,mscatmax.col>>
| `D[B[i,j], j] = max(D[B[i,j],j], A[i,j])`
| Elementwise column scatter-max
| <<udb:doc:inst:mscatmax_row,mscatmax.row>>
| `D[i, B[i,j]] = max(D[i,B[i,j]], A[i,j])`
| Elementwise scatter-max
| <<udb:doc:inst:mshift_ew,mshift.ew>>
| `D[i,j] = A[i, j+imm]`
| Elementwise row shift by constant offset
|===

==== Register move / data conversion

`<<udb:doc:inst:mmov_m_m,mmov.m.m>>` copies all elements from one `M` register to another.
`<<udb:doc:inst:mmov_a_m,mmov.a.m>>` copies all elements from an `Acc` register to an `M` register.
In both cases, the source and destination datatypes must match; if they differ, `amestatus.UN` is set and the destination is left unchanged.

`<<udb:doc:inst:mzero_2d,mzero.2d>>` writes zero to every element of an `Acc` register,
typically used to initialize an accumulator before a matrix multiply sequence.

[cols="1,2,3",options="header"]
|===
| Instruction | Operation | Description
| <<udb:doc:inst:mmov_a_m,mmov.a.m>>
| `D[i] = acc[i]`
| Elementwise unconditional move from accumulator to M register
| <<udb:doc:inst:mmov_m_m,mmov.m.m>>
| `D[i] = A[i]`
| Elementwise unconditional move between M registers
| <<udb:doc:inst:mzero_2d,mzero.2d>>
| `C = 0`
| Clear accumulator tile
|===

`<<udb:doc:inst:mconv_ew,mconv.ew>>` converts element data from one datatype to another.
An expand converts a single packed `M` register (packed dtype) into `pack_factor` consecutive
non-packed `M` registers; a compress does the reverse.

==== Scalars

In any AME implementation, software can use scalar broadcast instructions to use the same scalar
value with every element.
For example, software can implement elementwise add by a common constant using a scalar broadcast
followed by an elementwise add.

An optional extension, `Ztts`, provides instructions that take scalars as an input for elementwise
operations.
With `Ztts`, elementwise add by a common constant is a single instruction.

===== Scalar broadcast

AME provides two instructions to fill every element of an M register with a single scalar value,
analogous to `np.full_like`. The element size is determined by the register's programmed datatype.
Both non-packed and packed dtypes are supported; for packed dtypes all
`pack_factor * AME_NELEM` element positions are filled.

`<<udb:doc:inst:mbcast_x,mbcast.x>>` sources the scalar from an integer (`X`) register.
`<<udb:doc:inst:mbcast_f,mbcast.f>>` sources the scalar from a floating-point (`F`) register
and requires the F extension; software is responsible for placing the correctly-formatted
bit pattern for the destination datatype into the F register beforehand.

[cols="1,2,3",options="header"]
|===
| Instruction | Operation | Description
| <<udb:doc:inst:mbcast_x,mbcast.x>>
| `D[i] = xs1`
| Broadcast integer scalar to all elements of a square
|===

===== Scalar operand instructions

When a scalar is used as an operand, its type either comes from one of the AME datatype registers
or is pre-determined (for example, a constant exponent operand is always an XLEN-bit integer).

[cols="1,2,3",options="header"]
|===
| Instruction | Operation | Description
| <<udb:doc:inst:mbcast_x,mbcast.x>>
| `D[i] = xs1`
| Broadcast integer scalar to all elements of a square
|===

==== Load and store

The row-major and column-major interoperability instructions (`mls.rm`, `mls.cm`, `mss.rm`, `mss.cm`)
support both non-packed and packed datatypes. Data is always transferred as a single
contiguous block — no striding, partial-tile, or predicated variants exist.

For *non-packed datatypes* (size >= `AME_UNIT_DATATYPE_SIZE`), one square is transferred
between memory and one (or more, for wide types) M registers.

For *packed datatypes* (size < `AME_UNIT_DATATYPE_SIZE`), `pack_factor` squares are
transferred in a single instruction:

* **Load:** `pack_factor` squares are loaded from contiguous memory and packed into a
  single M register. Each sub-square contains `AME_NELEM` elements.
  In row-major format, the load accesses `sqrt(AME_NELEM)` rows and
  `pack_factor * sqrt(AME_NELEM)` columns in memory, where sub-square `sq` occupies columns
  `sq * sqrt(AME_NELEM)` to `(sq+1) * sqrt(AME_NELEM) - 1`.
  In column-major format, the load accesses `pack_factor * sqrt(AME_NELEM)` rows and
  `sqrt(AME_NELEM)` columns in memory, where sub-square `sq` occupies rows
  `sq * sqrt(AME_NELEM)` to `(sq+1) * sqrt(AME_NELEM) - 1`.

* **Store:** The single packed M register is unpacked and `pack_factor` squares are written
  to contiguous memory using the same layout.

The opaque-format instructions (`mls`, `mss`) transfer the raw M register contents without
interpreting the datatype and are correct for both non-packed and packed dtypes.

AME includes a mix of precise and imprecise exceptions.
When exceptions can occur, the precision is indicated. QQQ – more needed.
